using UnityEngine;
using System.Collections;

public class Door : MonoBehaviour 
{
    bool connected = false;

    //get a list of all rooms
    void getRoomList()
    {
        //find our level generator : This only works if this is a child of the generator
//        LevelGenerator root = this.GetComponent<Collider>().transform.root.gameObject;
    }
    
}
